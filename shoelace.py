print("Welcome!")

coordinates = input("Input list for coordinates.")

def shoelace(x):
    n = len(coordinates)
    for i <= n:
        j = 
        area1 = corners[i][0] * corners[j][1]
        area2 = corners[j][0] * corners[i][1]


area = abs(area1 - area2) / 2.0
return area
print(area)

